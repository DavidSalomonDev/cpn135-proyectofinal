version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    # Para Linux, usa esta configuración alternativa:
    #extra_hosts:
    #  - "host.docker.internal:host-gateway"
    restart: unless-stopped